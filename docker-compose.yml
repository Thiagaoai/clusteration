services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - panel-data:/data
    environment:
      DATABASE_URL: ${DATABASE_URL:-sqlite+aiosqlite:////data/vmpanel.db}
    restart: unless-stopped

volumes:
  panel-data:

